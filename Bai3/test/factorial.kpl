Program Factorial; (* Factorial *)

Var n : Integer;
    i : Integer;

Function F(n : Integer) : Integer;
  Begin
    If n = 0 Then F := 1 Else F := N * F (N - 1);
  End;

Begin
  i := READI;
  Call WriteLn;
  Call WriteI( F(i));
End. (* Factorial *)
